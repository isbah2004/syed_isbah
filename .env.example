# Mail sender address used for SMTP authentication and envelope from
EMAIL_FROM=youraddress@gmail.com

# Gmail app password generated for the account above
GMAIL_APP_PASSWORD=your-16-char-app-password

# Destination inbox for website messages (falls back to EMAIL_FROM if omitted)
EMAIL_TO=youraddress@gmail.com
